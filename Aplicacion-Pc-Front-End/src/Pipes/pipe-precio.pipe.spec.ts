import { PipePrecioPipe } from './pipe-precio.pipe';

describe('PipePrecioPipe', () => {
  it('create an instance', () => {
    const pipe = new PipePrecioPipe();
    expect(pipe).toBeTruthy();
  });
});
